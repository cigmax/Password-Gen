import os.path
import random
from tkinter import Y
class PassGen:


    #checks for if passwords already exist
    def FileCheck():
        return os.path.isfile('/codeHolder/PassList.txt')

    #for removal
    def FileRemove():
        os.remove('/codeHolder/PassList.txt')
        print("Passwords Expunged")
    #creation
    def FileCreater():
        with open("/codeHolder/PassList.txt", "w") as file:
            file.write("") #REQUIRES INDENTAITON 
    #adding
    def FileAppend(Password):
        with open("/codeHolder/PassList.txt", "a") as f:
            f.write("\n",Password)

    def GenerateCapLetter():
        return chr(random.randint(65,90))
    
    def GenerateLowLetter():
        return chr(random.randint(97,122))
    
    def GenerateSymbolSect1():
        return chr(random.randint(33,47))

    def GenerateSymbolSect2():
        return chr(random.randint(58,64))
    
    def GenerateSymbolSect3():
        return chr(random.randint(91,60))
    
    def GenerateSymbolSect4():
        return chr(random.randint(123,126))
    
    def GenerateNumber():
        return random.randint(0,9)
    
    def GenPassword(length, Letters, Numbers, CapLetters, SymbolOpt):
        Password = ""
        if Letters == Y and Numbers == N and CapLetters == N and SymbolOpt == N:
            for runs in range(length):
                #dont need random select between variations
                temp = GenerateLowLetter()  #watch this line for bugs
                Password =+ temp 
            return Password


    def PassOptionals():
        length = input(print("Enter the length of your desired password"))
        Letters = input(print("Do you want lower case letters in your password. Enter either Y or N"))
        while Letters != "Y" or "N":
            Letters = input(print("Invalid value entered. Do you want lower case letters in your password. Enter Y or N"))
        Numbers = input(print("Do you want numbers in your password. Enter either Y or N"))
        while Numbers != "Y" or "N":
            Numbers = input(print("Invalid value entered. Do you want Numbers in your password. Enter Y or N"))
        CapLetters = input(print("Do you want Capital letters in your password. Y or N"))
        while CapLetters != "Y" or "N":
            CapLetters = input(print("Invalid value entered. Do you want Capital letters in your password. Enter Y or N"))
        SymbolOpt = input(print("Do you want symbols in your password. Y or N"))
        while SymbolOpt != "Y" or "N":
            SymbolOpt = input(print("Invalid value entered. Do you want Symbols in your password. Enter Y or N"))
        return Letters, Numbers, CapLetters, SymbolOpt

    #testUpdate
    def Menu():
        print("Select from the options below\n1.Generate Password \n2.Preview previous passwords \n3.exit")

    choice = input()

   # match choice:
    #    case 1.








choice = int(input())

